
spring:
  datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/테스트1?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
        username: root
        password: kik@0811012
  #        url: jdbc:h2:tcp://localhost/~/post
#        username: sa
#        password: 0811
#        driver-class-name: org.h2.Driver
  jpa:
    hibernate:
     ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    defer-datasource-initialization: true
    database-platform=org.hibernate.dialect: MySQL5InnoDBDialect
  sql:
    init:
      mode: always

logging.level:
  org.hibernate.SQL: debug
# org.hibernate.type: trace